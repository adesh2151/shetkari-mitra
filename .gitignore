# dependencies
node_modules/

# build output
dist/

# capacitor / android
android/
.gradle/

# secrets (never commit) — Option B needs none, but keep the guard
.env
.env.local
*.local

# python training env + downloaded datasets (never commit these)
model-training/.venv/
model-training/data/
tensorflow_datasets/

# editor / os
.DS_Store
.vscode/
.idea/
*.log
